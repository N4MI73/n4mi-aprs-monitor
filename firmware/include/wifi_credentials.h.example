#pragma once

// Copy this file to wifi_credentials.h and fill in real values.
// wifi_credentials.h is gitignored -- never commit real credentials.
// This is a deliberate temporary stand-in for the real captive-portal
// setup, same pattern n4mi-propagation-monitor used before its own portal
// was built.

#define WIFI_SSID "your-network-name"
#define WIFI_PASSWORD "your-network-password"
